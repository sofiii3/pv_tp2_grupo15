const lado = 5;

const calcularArea = (l) => 2 * l;
const calcularPerimetro = (l) => l + l + l + l;

console.log("Área del cuadrado:", calcularArea(lado));
console.log("Perímetro del cuadrado:", calcularPerimetro(lado));
